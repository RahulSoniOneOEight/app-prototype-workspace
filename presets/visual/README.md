# Visual Presets

Reusable visual-direction intelligence. Client-specific ranking and selected tokens belong under the client workspace, not here.

Each family has exactly three curated options: A = safest, B = differentiated, C = exploratory. Source colors are kept separate from semantic UI tokens. Semantic mappings are seeds and must be contrast-validated before production.
